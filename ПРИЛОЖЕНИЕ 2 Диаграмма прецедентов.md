# ПРИЛОЖЕНИЕ 2. Диаграмма компонентов 

```mermaid
flowchart LR

    Guest(("Гость"))
    User(("Покупатель"))
    Support(("Оператор поддержки"))
    Content(("Контент-менеджер"))
    Manager(("Менеджер заказов"))
    Admin(("Администратор"))

    subgraph System["АС «ВкусВилл Mobile»"]

        Catalog(["Просмотр каталога и товаров"])
        Search(["Поиск товаров"])
        Account(["Регистрация и управление профилем"])
        Favorites(["Список покупок и избранное"])
        Cart(["Работа с корзиной"])
        Order(["Оформление заказа"])
        Tracking(["Отслеживание заказа"])
        Loyalty(["Программа лояльности"])
        Review(["Отзывы"])
        SupportReq(["Обращение в поддержку"])

        Products(["Управление товарами"])
        Orders(["Обработка заказов"])
        Users(["Управление пользователями"])
        Notifications(["Управление уведомлениями"])
    end

    Guest --> Catalog
    Guest --> Search
    Guest --> Account

    User --> Catalog
    User --> Search
    User --> Account
    User --> Favorites
    User --> Cart
    User --> Order
    User --> Tracking
    User --> Loyalty
    User --> Review
    User --> SupportReq

    Support --> SupportReq
    Content --> Products
    Manager --> Orders
    Admin --> Users
    Admin --> Notifications
    ```