# ПРИЛОЖЕНИЕ 3. Диаграмма классов

```mermaid
classDiagram

    class User {
        +String id
        +String phone
        +String name
        +String email
        +register()
        +authorize()
        +updateProfile()
    }

    class Address {
        +String id
        +String city
        +String street
        +String house
        +String apartment
        +String type
    }

    class Category {
        +String id
        +String name
        +String parentId
    }

    class Product {
        +String id
        +String name
        +Decimal price
        +String description
        +String composition
        +Decimal rating
        +Boolean available
    }

    class Favorite {
        +String id
        +DateTime createdAt
        +add()
        +remove()
    }

    class ShoppingList {
        +String id
        +String name
        +addProduct()
        +removeProduct()
    }

    class Cart {
        +String id
        +Decimal totalAmount
        +calculateTotal()
        +addItem()
        +removeItem()
        +clear()
    }

    class CartItem {
        +Int quantity
        +Decimal price
        +Decimal discount
    }

    class Order {
        +String id
        +DateTime createdAt
        +OrderStatus status
        +Decimal totalAmount
        +String deliveryType
        +calculateTotal()
        +submit()
        +cancel()
    }

    class OrderItem {
        +Int quantity
        +Decimal price
        +Decimal discount
    }

    class Payment {
        +String id
        +Decimal amount
        +String method
        +PaymentStatus status
        +process()
        +cancel()
    }

    class Delivery {
        +String id
        +String type
        +String address
        +DateTime plannedTime
        +DeliveryStatus status
    }

    class LoyaltyAccount {
        +String id
        +Int bonusPoints
        +accruePoints()
        +spendPoints()
    }

    class Review {
        +String id
        +Int rating
        +String text
        +DateTime createdAt
        +create()
        +edit()
    }

    class Notification {
        +String id
        +String type
        +String message
        +DateTime createdAt
        +send()
    }

    class SupportRequest {
        +String id
        +String subject
        +String text
        +String status
        +create()
        +close()
    }

    User "1" --> "0..*" Address : сохраняет
    User "1" --> "0..*" Order : оформляет
    User "1" --> "1" Cart : имеет
    User "1" --> "0..*" Favorite : формирует
    User "1" --> "0..*" ShoppingList : создаёт
    User "1" --> "1" LoyaltyAccount : имеет
    User "1" --> "0..*" Review : оставляет
    User "1" --> "0..*" Notification : получает
    User "1" --> "0..*" SupportRequest : создаёт

    Category "1" --> "0..*" Product : содержит

    Favorite "1" --> "1" Product : содержит
    ShoppingList "1" --> "0..*" Product : содержит

    Cart "1" --> "1..*" CartItem : содержит
    CartItem "0..*" --> "1" Product : ссылается на

    Order "1" --> "1..*" OrderItem : содержит
    OrderItem "0..*" --> "1" Product : ссылается на

    Order "1" --> "1" Payment : имеет
    Order "1" --> "0..1" Delivery : получает
